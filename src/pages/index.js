import Image from 'next/image'
import { Inter } from 'next/font/google'


export default function Home() {
  return (
    <main>

    </main>
     
  )
}

export async function getServerSideProps (context) {
  const movies = await m
}
